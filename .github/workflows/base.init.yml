name: app

on:
  push:
    paths:
    - Dockerfile.base.init
    - .github/workflows/base.init.yml

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [13.x]
        docker-repository: [powerfulyang/powerfulyang.com.base]
    steps:
      - uses: actions/checkout@v2
      - name: Dockerize
        id: dockerize
        uses: elgohr/Publish-Docker-Github-Action@master
        with:
          name: ${{ matrix.docker-repository }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          tags: "latest"
          dockerfile: Dockerfile.base.init
          context: .
